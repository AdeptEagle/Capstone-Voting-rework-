# Railway Environment Variables Template
# Copy these to your Railway project environment variables

# Database (Railway will provide this automatically)
# DATABASE_URL="postgresql://username:password@host:port/database"

# JWT Secret (CHANGE THIS IN PRODUCTION!)
JWT_SECRET="your-super-secure-jwt-secret-key-change-this"

# Application
NODE_ENV="production"
PORT=8080

# Frontend URL (update with your frontend Railway URL)
FRONTEND_URL="https://your-frontend-app.railway.app"

# Email Service Configuration
EMAIL_SERVICE="brevo"  # Options: gmail, brevo, sendgrid

# Brevo SMTP Configuration (if EMAIL_SERVICE=brevo) - RECOMMENDED
BREVO_SMTP_LOGIN="99936e001@smtp-brevo.com"
BREVO_SMTP_KEY="your-brevo-smtp-key"
BREVO_SENDER_EMAIL="noreply@ballotblitz.com"  # Verified sender email in Brevo

# Gmail SMTP Configuration (if EMAIL_SERVICE=gmail)
GMAIL_USER="your-gmail@gmail.com"
GMAIL_PASSWORD="your-16-character-app-password"

# SendGrid Configuration (if EMAIL_SERVICE=sendgrid)
# SENDGRID_API_KEY="your-sendgrid-api-key"
